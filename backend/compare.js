/* Created by Kammar1006 */

const compareSRC = (p, r) => {
    return true;
}

const compareDES = (p, r) => {
    return true;
}

const compareProtocol = (p, r) => {
    return true;
}

module.exports = {compareSRC, compareDES, compareProtocol}